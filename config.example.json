{
  "_comment": "Example shape of config.json. The real file is created automatically on first run with generated credentials — you normally never edit it by hand; use the web GUI. If you do edit it, stop the service first (the GUI overwrites hand edits).",
  "admin_username": "admin",
  "admin_password_hash": "(generated on first run — a hash, never the plaintext password)",
  "session_secret": "(generated on first run)",
  "bind_host": "0.0.0.0",
  "bind_port": 8080,
  "poll_interval_seconds": 300,
  "cloudflare_api_token": "(set via the web GUI — token scoped to Zone / DNS / Edit; leave blank for notification-only mode)",
  "cloudflare_zone_id": "(32-char hex ID from the Cloudflare zone Overview page)",
  "ddns_tracked_record_ids": ["(Cloudflare record IDs toggled on in the DNS Records page)"],
  "discord_webhook_urls": [
    {"url": "https://discord.com/api/webhooks/...", "ping_user_ids": ["123456789012345678"]},
    {"url": "https://discord.com/api/webhooks/... (every webhook gets each notification)", "ping_user_ids": []}
  ],
  "smtp": {
    "host": "mail.yourprovider.com",
    "port": 587,
    "security": "starttls",
    "username": "ddns@yourdomain.com",
    "password": "(set via the web GUI)",
    "from_addr": "ddns@yourdomain.com",
    "to_addrs": ["you@yourdomain.com", "partner@example.com"]
  },
  "notify_ipv4_changes": {"discord": true, "email": true},
  "notify_ipv6_changes": {"discord": true, "email": true},
  "notify_on_errors": {"discord": true, "email": true},
  "summary": {
    "enabled": false,
    "frequency": "weekly (or: daily | biweekly | monthly)",
    "day_of_week": "6 (0=Monday … 6=Sunday; used for weekly/biweekly)",
    "day_of_month": "1 (1–28; used for monthly)",
    "time": "01:00 (24-hour, server local time)",
    "catch_up": "true = a summary missed while the service was off is sent right after startup; false = skip it (the next one covers the gap)",
    "discord": true,
    "email": true
  }
}
